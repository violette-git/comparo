/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Configure any environment variables, redirects, or rewrites here
  env: {
    // You can add environment variables here if needed
  },
}

module.exports = nextConfig